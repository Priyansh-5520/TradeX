# Leave unset locally: Vite proxies /api to the Express server on port 5000.
# Set this to the deployed backend URL when frontend and backend use different origins.
VITE_API_URL=

# Must be the same OAuth web-client ID configured on the backend.
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
